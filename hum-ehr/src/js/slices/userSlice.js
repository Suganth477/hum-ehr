// Redux slice disabled for the current phase because migrated screens do not use Redux.
// Re-enable only when you add Redux Provider back in main.jsx.
export default function userReducer(state = {}, action = {}) {
	return state;
}
